//https://davidwalsh.name/es6-generators
//Introduction to generator functions. Instead of normal functions which run to completion, generator functions can start and stop arbitrarily depending on external instructions.
  //Seems to be part of the data stream model of computing.

//https://github.com/Reactive-Extensions/RxJS/blob/master/doc/gettingstarted/generators.md
  //generators yield statements do not terminate until told to do so from the outside, via calling the next() method.
